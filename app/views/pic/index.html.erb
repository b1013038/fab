<% provide(:title, "メインページ") %>
<%= render 'pic/header' %>
<div id="container">
  <%= render 'pic/sidbar' %>
  <% flash.each do |key, value| %>
  <%= content_tag(:div, value, class: "#{key}") %>
  <% end %>
  <br>
  <div id="content">
    <h1>せつめい</h1>
aaaaaaaa
    <h1>新着順 最新10件</h1>
    <% @img.reverse_each do |img| %>
    <ul class="thumbnail clearfix">
      <li>
	<%= link_to (image_tag "/img/#{img.title}.png", size: "150x150"), action: 'download', id: img.id %>
	<%= link_to img.title.split("_")[0], action: 'download', id: img.id %>
      </li>
    </ul>
    <% end %>
    safari9.0.1, chrome46.0.2490.86 (64-bit),firefox41.0.1で正常に表示確認IEは多分無理
    </div>
</div>
<%= render 'pic/footer' %>
